import React from 'react';
import style from './style.scss';

const UnfinishedActivity = () => {
  return (
    <div className={style.wrapper}>
      <dl className={style.header}>
        <dt className={style.title}>
              UnfinishedActivity
        </dt>
      </dl>
    </div>
  );
};

export default UnfinishedActivity;
